import React, { useRef, useState } from 'react'
import { Text, View, Image, StyleSheet } from 'react-native'

const Cart = ({ item, navigation }) => {
    return (
        <View style={styles.categorieBox}>
            <Text>Carrinho</Text>
        </View>
    )
}

const styles = StyleSheet.create({

});

export default Cart