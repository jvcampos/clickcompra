import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  input: {
    marginHorizontal: 30,
    marginVertical: 10,
  },
  button: {
    marginVertical: 10,
  }
})

export default styles